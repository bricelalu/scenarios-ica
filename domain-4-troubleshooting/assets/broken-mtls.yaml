apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: broken-mtls
spec:
  selector:
    matchLabels:
      app: httpbin
  mtls:
    mode: STRICT  # ERROR: Forces mTLS but clients may not be configured